{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    // only use this connection string for migrations
    // "GymDB": "Server=localhost;Database=gymnasium;Port=5432;User Id=myuser;Password=root",

    // use this connection string for everything else
    "GymDB": "Server=db;Database=gymnasium;Port=5432;User Id=myuser;Password=root"
  }
}
